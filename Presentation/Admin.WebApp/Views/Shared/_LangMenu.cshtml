@using Admin.Web.Common.Helpers
@if (ViewBag.Cultures != null && ((IList<string>)ViewBag.Cultures).Count > 1)
{
<ul class="nav nav-pills pull-right">
    @foreach (string s in (IList<string>)ViewBag.Cultures)
    {
        @Url.LangSwitcher(s, ViewContext.RouteData, s)
    }
</ul>
}
